{
  "name": "sdk-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.19.0",
  "scripts": {
    "build": "pnpm -r --filter './packages/*' build",
    "typecheck": "pnpm -r --filter './packages/*' typecheck",
    "test": "pnpm --filter @lunya/sdk test"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
